// Jest setup file - using CommonJS to avoid ESM transformation issues
require('@testing-library/jest-dom');

// Explicitly mark as CommonJS module to prevent TypeScript from adding export {}
module.exports = {};
